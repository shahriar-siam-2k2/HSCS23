#include <iostream>
#include <math.h>
using namespace std;

int main(){
    double num1, a, b;
    cout << endl;
    cin >> a >> b;
    num1 = pow(a, b);
    cout << endl << "a to the power b is " << num1 << endl;
    return 0;
}